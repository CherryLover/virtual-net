# virtual-net

浏览器里的网络规划与验证沙盘。项目总纲在 `ROADMAP.md`，所有阶段目标、范围和完成标准以它为准。

## 工作方式

- 开始任何检查点之前，先读 `ROADMAP.md` 对应章节和 `docs/checkpoints/` 里的细化文档
- 引擎先于界面：功能先在 `packages/engine` 里有 vitest 测试，再接到 `apps/web`
- 验证结果必须带原因，只说"不通"不算完成
- 界面文案极简，只说做什么。错误提示例外，必须说清楚
- 检查点打勾前，页面实际点一遍验收场景
- 改动路线图范围要在其「变更记录」留一笔

## 沟通

- 用中文
- 汇报结果说用户能看到什么，不说实现细节
