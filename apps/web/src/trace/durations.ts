/** 时间线基准时长（1× 播放）与总长上限，来自 CP3 文档 2.1 */

/** 各类片段的基准时长，单位毫秒 */
export const BASE_MS = {
  /** 普通停留 */
  dwell: 700,
  /** 中间设备重新发出（新包）时的停留 */
  renew: 900,
  /** 起点收到应答的停留 */
  receive: 500,
  /** 验证终止时的停留 */
  stop: 1200,
  /** 沿一根连线移动 */
  travel: 900,
  /** 阶段之间的停顿 */
  gap: 600,
} as const;

/** 1× 播放的总长上限；超过后所有片段等比例缩短到恰好这个值 */
export const MAX_TOTAL_MS = 30_000;
