# virtual-net 路线图

> 状态：进行中 · 建档 2026-09-04
> 这是项目总纲。阶段目标、范围、完成标准都以这里为准。改动请在文末「变更记录」留一笔。

## 一句话

在浏览器里搭一个"虚拟配网"沙盘：拖设备、连网线、配 IP，然后直接验证"能不能通、在哪断、为什么"。
以通用方式表达家庭、工作室和小公司的网络：终端、网络设备、服务器、访问控制、代理与虚拟网络。地域和具体产品只作为可选场景，不决定基础模型。

> 2026-09-07 实施更新：UX、CP4 与 CP5 首批已完成，实际文件导入验收已补齐。详见 [通用网络模型与页面优化](docs/design/network-model-and-ui.md)、[CP4](docs/checkpoints/CP4-access-control.md)、[CP5 首批](docs/checkpoints/CP5-proxy-services.md)。CP4 旧细化文档与旧验收用例仅供历史参考。CP5 后续转发能力、CP6、CP7 不在本轮范围。

> 最新增量：CP5 第二批 [SOCKS5 UDP DNS 查询](docs/checkpoints/CP5-udp-dns.md) 已完成本地验收，尚未部署；其他后续能力仍未实施。

## 面向谁

- 家里喜欢折腾网络的人
- 工作室 / 小公司的网络运维
- 做跨境业务的公司

## 第一期不做什么

- 不做登录和账号。打开页面就能用，数据存在浏览器本地
- 不导出真实设备的配置文件。只在页面上管理和查看
- 不模拟命令行。全部表单配置
- 不模拟带宽、延迟、丢包。"能通"只代表配置逻辑上通得过
- 不做动态路由协议（OSPF / BGP / 生成树）

## 怎么读这份文档

- 「总览」里一个大 checkbox 就是一个**检查点**，代表一个能单独演示的阶段目标
- 每个检查点的详情包含：**目标**、**拆分步骤**、**完成标准**
- 完成标准全部满足、引擎自动测试通过、页面实际点过一遍，才能打勾
- 每个检查点动手前，先在 `docs/checkpoints/` 下写细化文档把小功能拆细，讨论完再开工。文档规范见 [docs/checkpoints/README.md](docs/checkpoints/README.md)
- 细化文档里每个步骤都带验收标准。所有验收标准汇总成一份独立的测试表 [docs/TEST-PLAN.md](docs/TEST-PLAN.md)，从上到下逐条核对
- 顺序可以调、范围可以改，但要记进「变更记录」

---

## 总览

- [x] **CP0 项目骨架** — 仓库、引擎与网页两个包、测试跑通、空画布能打开 · [细化文档](docs/checkpoints/CP0-project-skeleton.md)
- [x] **CP1 局域网基础** — 电脑 ↔ 路由器 ↔ 外网能通，配错能报出来 · [细化文档](docs/checkpoints/CP1-lan-basics.md)
- [x] **CP2 交换机与多设备** — 交换机、VLAN、光猫、无线 AP、多台电脑 · [细化文档](docs/checkpoints/CP2-switching-and-devices.md)
- [x] **CP3 路径追踪与动画** — 逐跳显示数据包怎么走、在哪停、为什么 · [细化文档](docs/checkpoints/CP3-trace-and-animation.md)
- [x] **CP4 网络区域与访问控制** — 通用区域、访问规则、目标服务、DNS 应答处理；整体验收通过 · [细化文档](docs/checkpoints/CP4-access-control.md)
- [ ] **CP5 服务器、代理与转发** — 首批服务器与 HTTP / CONNECT / SOCKS5 TCP 代理、第二批 SOCKS5 UDP DNS 查询已完成；隧道、多级转发等仍属后续，整个 CP5 尚未完成 · [首批](docs/checkpoints/CP5-proxy-services.md) · [第二批](docs/checkpoints/CP5-udp-dns.md)
- [ ] **CP6 虚拟网络与网关组合** — 虚拟化主机、网桥、虚拟设备与网关职责
- [ ] **CP7 从外面进来** — 端口转发、公网 IP 判断、从外部访问家里的 NAS
- [x] **UX 页面与连线优化** — 保留主画布，外围布局、右侧面板、可拖拽曲线与端口位置已完成；文件往返及交互验收通过 · [设计与决定](docs/design/network-model-and-ui.md)

---

## CP0 项目骨架

> 细化文档：[docs/checkpoints/CP0-project-skeleton.md](docs/checkpoints/CP0-project-skeleton.md)

**目标**：把地基打好。一条命令能启动网页看到空画布，一条命令能跑引擎测试。

**拆分步骤**
- [ ] pnpm workspace，两个包：`packages/engine`（纯逻辑）和 `apps/web`（网页）
- [ ] 引擎：TypeScript + vitest，放一个占位测试
- [ ] 网页：React + Vite + React Flow，打开是一块空画布和一个空的左侧设备栏
- [ ] 统一的代码格式化与检查
- [ ] README 写清楚怎么启动、怎么测试

**完成标准**
1. `pnpm dev` 打开浏览器看到空画布
2. `pnpm test` 通过
3. 目录结构和本文档「目录结构」一节一致

---

## CP1 局域网基础

> 细化文档：[docs/checkpoints/CP1-lan-basics.md](docs/checkpoints/CP1-lan-basics.md)

**目标**：最小可用闭环。一台电脑、一台路由器、一个外网。电脑能 ping 通路由器，能 ping 通外网，能打开网站。配错了能指出哪里错。

**拆分步骤**

数据模型
- [ ] 设备、端口、连线、IP 配置的统一数据结构，整张图能序列化成一份 JSON
- [ ] 第一批设备：电脑、路由器、互联网

画布
- [ ] 从左侧设备栏拖设备进画布
- [ ] 在两个端口之间拉线，线上显示端口名
- [ ] 选中、移动、删除设备和连线
- [ ] 点设备打开右侧配置面板

配置面板
- [ ] 电脑：IP、掩码、网关、DNS。手动填 / 自动获取
- [ ] 路由器：LAN 口 IP 和掩码、DHCP 开关与地址池、WAN 口（自动获取公网地址）、NAT 默认开
- [ ] 互联网：内置 DNS 地址与两个示例网站。本阶段一律视为可达，不按地域分类

引擎
- [ ] ARP 与直连二层转发
- [ ] IP 转发与默认路由
- [ ] NAT（源地址转换）
- [ ] DHCP 分配
- [ ] 每一步都产出"决策记录"，供结果面板和后续动画使用

验证
- [ ] ping：任选两点，返回 通 / 不通、断在哪、原因
- [ ] 访问网站：电脑到某个目标网站，先 DNS 再连接
- [ ] 静态检查：IP 冲突、网关不在本网段、没配网关、一根线两端不在同一网段、掩码不一致、DHCP 地址池与静态地址重叠
- [ ] 结果面板：列出所有问题，点一条定位到对应设备

保存
- [ ] 浏览器本地自动保存，刷新不丢
- [ ] 导出 JSON、导入 JSON
- [ ] 新建 / 清空

**完成标准（验收场景）**
1. 空画布拖入 1 电脑 + 1 路由器 + 1 互联网，连两根线。电脑设自动获取，路由器开 DHCP。ping 路由器通，ping 8.8.8.8 通，打开示例网站成功
2. 把电脑网关改成别的网段。静态检查报"网关不在本网段"，ping 8.8.8.8 不通，原因指向网关
3. 关掉路由器 NAT。ping 外网不通，原因说明私网地址无法回程
4. 导出 JSON，清空画布，再导入，图和配置完整回来
5. 以上场景在引擎里各有一条自动测试

---

## CP2 交换机与多设备

> 细化文档：[docs/checkpoints/CP2-switching-and-devices.md](docs/checkpoints/CP2-switching-and-devices.md)

**目标**：能搭出一个真实的家庭或小办公室网络。

**拆分步骤**
- [ ] 二层交换机：多口、MAC 地址学习、未知目标泛洪
- [ ] VLAN：access 口 / trunk 口。路由器多 LAN 口或单臂路由
- [ ] 无线 AP：本阶段当作带 SSID 标签的交换机，无线客户端按有线处理
- [ ] 光猫：桥接 / 路由两种模式。拨号在光猫还是在路由器。给的是公网 IP 还是运营商内网地址
- [ ] DHCP 多个地址池，每个 VLAN 一池
- [ ] 静态检查：VLAN 两端不一致、trunk 漏放行、双层 NAT 提示、同一网段两个 DHCP 服务
- [ ] 画布：设备多了以后的对齐、框选、批量移动

**完成标准（验收场景）**
1. 光猫桥接 + 路由器拨号 + 交换机 + 3 台电脑 + 1 个 AP，全部互通且能上外网
2. 光猫改成路由模式，静态检查提示双层 NAT
3. 两个 VLAN 各挂一台电脑，同 VLAN 通，跨 VLAN 不通。加上单臂路由后跨 VLAN 通

---

## CP3 路径追踪与动画

> 细化文档：[docs/checkpoints/CP3-trace-and-animation.md](docs/checkpoints/CP3-trace-and-animation.md)

**目标**：把"为什么通 / 为什么不通"变成看得见的东西。这是 Packet Tracer 最有价值的部分。

**拆分步骤**
- [ ] traceroute：逐跳列表
- [ ] 动画：数据包沿连线移动，逐跳停留，失败点高亮
- [ ] 点某一跳看包头：源 / 目的 MAC、源 / 目的 IP、NAT 前后、VLAN 标签
- [ ] 每一跳的决策解释：查路由表命中哪条、ARP 查到谁、NAT 做了什么映射
- [ ] 播放 / 单步 / 调速

**完成标准（验收场景）**
1. CP1 场景 1 的 ping 8.8.8.8 能逐跳播放，路由器那一跳显示 NAT 前后地址
2. CP1 场景 2 的失败 ping 停在电脑上，解释是"网关不可达"

---

## CP4 网络区域与访问控制

> 当前依据：[CP4 细化文档](docs/checkpoints/CP4-access-control.md)、[设计文档第 2–3 节](docs/design/network-model-and-ui.md)。[旧细化文档](docs/checkpoints/CP4-zones-and-border.md)仅供历史参考，不再作为实施或验收依据。

**目标**：以通用规则解释某次访问为什么允许或拒绝，不依赖地域或固定网站名单。

**拆分步骤**
- [x] 网络区域与目标服务：可命名区域、可配置目标；位置只影响展示
- [x] 访问控制：实际经过的设备逐台检查，规则支持允许、拒绝和明确的默认动作
- [x] 按已确定的首条匹配、默认动作和连接状态执行返回规则；只匹配所在连接的可见信息
- [x] DNS 服务记录与查询路径分开建模，区分服务自身应答和途中应答改写
- [x] 诊断显示命中设备、规则、原目标与实际结果，不把应答改写直接等同于无法连接

**验收场景（已通过）**
1. 同一访问只因规则开关变化而允许或拒绝，说明命中的规则
2. 两台访问控制设备均按路径生效，未经过的设备不影响结果
3. DNS 应答改写与后续连接分别展示；拖动节点不改变结果

---

## CP5 服务器、代理与转发

> 首批依据：[CP5 服务器与代理服务](docs/checkpoints/CP5-proxy-services.md)、[设计文档第 4 节](docs/design/network-model-and-ui.md)。下面明确区分已实现的首批与未实施的后续能力，不以首批通过代表整个 CP5 完成。

**目标**：用户可从左侧添加“代理”实体，选择通过它访问目标，并看到客户端到代理、代理到目标两段连接的结果。

**首批：已完成，整体验收通过**
- [x] 服务器与服务基础：地址、监听端口、启停、DNS 服务；代理认证与通用访问限制
- [x] 左侧独立代理入口；节点可连线、配置、保存和诊断，内部复用主机能力
- [x] HTTP 正向代理、CONNECT、SOCKS5 的 TCP 连接
- [x] 单次访问显式选代理；HTTP 由代理解析，CONNECT / SOCKS5 可选客户端或代理解析；失败不自动直连
- [x] 分段追踪与原因：入口不可达、认证失败、解析失败、出口不可达分别定位

**第二批：已完成**
- [x] [SOCKS5 UDP 与 DNS 查询](docs/checkpoints/CP5-udp-dns.md)：独立控制与 UDP 中继、DNS 出口及返回路径、两个验证入口、配置保存及失败定位。

**后续：尚未实施，不属于本轮交付**
- [ ] UDP 的其他应用、设备级分流、加密传输与网络隧道
- [ ] 多级代理、中转与 IP 轮换，需另行细化验收，不把所有代理等同于隧道

**首批验收场景**
1. 左侧拖入代理，连接与配置后显式选择使用，访问展示两段完整路径
2. 错误认证、关闭入口端口、阻断代理出口分别给出不同失败原因
3. 本地解析和代理端解析显示不同查询位置，普通 ping 不因应用代理而自动改道
4. 图中存在但未被选用的代理不影响直接访问，也不自动消除告警

---

## CP6 虚拟网络与网关组合

**目标**：用通用虚拟化主机、网桥与网关职责，表达虚拟设备和多级转发，不绑定具体产品。

**拆分步骤**
- [ ] 虚拟化主机：物理接口、网桥、虚拟设备及其网卡，支持 VLAN
- [ ] 内部视图编辑虚拟连接，外部视图呈现物理接口
- [ ] 虚拟网关连接上游与局域网，或作为现有网关之外的额外转发节点
- [ ] 复用 CP5 服务器与服务能力表达文件服务等设备
- [ ] 检查网桥误接、网关自指、地址分配冲突和回程路径问题

**验收候选（细化文档待写）**
1. 虚拟网关正确连接后，客户端能访问目标服务
2. 额外网关承担指定代理转发，路径显示其职责
3. 网桥或网关配置错误时定位到实际错误位置

---

## CP7 从外面进来

**目标**：回答"在外面能不能访问家里的 NAS"。

**拆分步骤**
- [ ] 路由器端口转发规则
- [ ] 公网 IP 判断：光猫给的是公网还是运营商内网
- [ ] 从其他网络的设备发起访问目标服务端口
- [ ] 运营商内网时提示无法直连，建议内网穿透（第二期）

**完成标准（验收场景）**
1. 有公网 IP + 端口转发正确，外部访问 NAS 成功
2. 运营商内网地址，外部访问失败，原因指向运营商内网

---

## UX 页面与连线优化

> 独立工作包，已完成交互与文件往返验收；详见 [设计文档第 5–8 节](docs/design/network-model-and-ui.md)。不推翻已完成阶段，不替换用户满意的主画布。

- [x] 工具栏、左侧分组与搜索、可收起面板和统一排版
- [x] 右侧节点面板的信息分组、字段校验、配置与验证切换
- [x] 贝塞尔曲线控制柄拖拽、恢复自动线形、保存与撤销
- [x] 交换机等节点上下接线位置优化，端口身份与占用语义不变
- [x] 连线标签、路径动画、旧文件兼容和窄屏回归

验收项目见 [测试表新增节](docs/TEST-PLAN.md#方向调整与页面优化验收候选)。已采用两个端点相对控制柄、逻辑端口独立选边、配置 / 端口 / 验证分组与失焦校验提交；桌面、窄屏、存储和动画整体验收通过。

## 本轮证据与兼容

- 2026-09-07 引擎最终范围审计：22 个测试文件、145 项测试全部通过，类型、格式与差异检查通过。原 144 项审计后补充规则名称快照与精确定位测试；覆盖旧场景、双控制串联、连接返回、DNS 改写到有效本地服务、三协议与凭据导入重放。测试数仅指引擎，不混计网页测试。
- 浏览器已实际跑通三协议、新代理接线转发、错误认证、规则拒绝与精确定位、DNS 改写后访问真实服务、三尺寸布局、曲线拖动/撤销/冷刷新和实际贴线动画。CP1 网关错误与恢复、CP2 VLAN 隔离均回归通过。
- 最终全量：引擎 145 项、网页 103 项，共 248 项测试通过；lint、typecheck、build 通过。用户开放文件权限后，实际文件导入、再次导出比对、撤销/重做、冷刷新及代理访问已验证；逐项证据见 [测试表](docs/TEST-PLAN.md)。
- 兼容只承诺新程序读取旧拓扑；旧文件缺少的新字段采用原有行为。旧程序读取含新设备或展示字段的文件不作保证。
- 模拟账号密码随本地保存与 JSON 导出明文保留，只能填写测试数据，不得填真实凭据。本轮不涉及后端、真实代理转发或部署。

## 第二期候选（不排期）

- 内网穿透：frp、Cloudflare Tunnel、Tailscale / ZeroTier 组网
- IPv6
- 双 WAN、负载与故障切换
- 跨境公司多站点：两地内网通过 WireGuard 站点互联
- 线路质量：延迟、丢包、运营商出境线路差异
- 场景模板库：一键载入典型家庭 / 工作室 / 跨境小公司
- 云端保存与分享链接（接 Pocket ID）

---

## 技术选型（暂定，可讨论）

| 部分 | 选择 | 理由 |
| --- | --- | --- |
| 语言 | TypeScript | 引擎和网页共用类型，数据格式只定义一次 |
| 仓库 | pnpm workspace | `packages/engine` + `apps/web` |
| 引擎 | 纯 TypeScript，不依赖界面 | 可测试。每个验收场景就是一条测试 |
| 测试 | vitest | 快，和 Vite 同源 |
| 网页 | React + Vite | 生态成熟 |
| 画布 | React Flow（@xyflow/react） | 节点、连线、拖拽、缩放都是现成的 |
| 状态 | Zustand | 轻，够用 |
| 存储 | IndexedDB + JSON 文件 | 无后端，本地优先 |
| 部署 | Cloudflare Workers 静态资源，https://vnet.flyooo.uk | 无后端，零维护；`pnpm deploy` 一条命令发布 |

## 目录结构（预期）

```
virtual-net/
  ROADMAP.md              本文档
  README.md
  docs/
    checkpoints/          每个检查点的细化文档（规范见其 README.md）
    design/               数据模型、引擎设计
    TEST-PLAN.md          测试表，汇总所有验收标准
  packages/engine/        模拟引擎
  apps/web/               网页
```

## 工作约定

- 引擎先于界面。每个功能先在引擎里有测试，再接界面
- 验证结果必须带原因。只说"不通"不算完成
- 界面文案极简。只说做什么，不解释为什么。错误提示例外，必须说清楚
- 每个检查点打勾前，页面实际点一遍验收场景
- 改路线图要记变更记录

## 变更记录

- 2026-09-07 需求文档先提交为 ea60e29，随后按 CP5 顺序细化并完成 SOCKS5 UDP DNS 查询批次；282 项自动测试、构建、桌面/窄屏及实际文件往返通过。仅该批次完成，CP5 整体仍未完成，未部署或提前启动文末后续需求。

- 2026-09-07 在文末追加后续需求讨论：面向网络工程师与企业运维的服务协议、诊断与故障演练方向，以及节点复制和持久分组；明确 MCP 暂不加入。随后补充画布导出 SVG、PNG、PDF 的需求。仅完善需求，不启动实现、不改变当前检查点状态和执行顺序。

- 2026-09-07 补齐真实文件导入验收，完成 CP4、CP5 首批与 UX；保留 CP5 整体未完成状态。按试用反馈缩短顶部名称框、把验证入口改成次要图标，步骤序号移出节点且不受选中层级遮挡；播放条与缩放按钮错开，适配画布增加步骤留白。未启动后续阶段或部署。

- 2026-09-07 用户授权实施后，完成 UX、CP4、CP5 首批代码并进入整体交互验收；明确规则、解析顺序、代理表示和兼容决定。新 CP4 / CP5 细化文档取代旧待定入口，引擎 145 项测试通过，浏览器三类代理已跑通。未提前完成整个 CP5；隧道等后续能力、CP6 / CP7 不在本轮。以下“仅文档更新”条目为同日更早的历史记录。

- 2026-09-07 用户确认通用网络模型方向、左侧独立“代理”实体和页面优化需求。修订 CP4–CP6 表述与范围，新增 UX 工作包与设计文档；旧 CP4 方案及 53 条用例标为历史待替换。保留 CP0–CP3 完成状态与检查点编号；协议批次、端口与曲线交互细节尚待确认。本次仅文档更新，未实现或部署。

- 2026-09-04 建档。确定第一期范围、八个检查点、技术选型
- 2026-09-04 CP0–CP3 细化文档完成（`docs/checkpoints/`），每步带验收标准；建立测试表 `docs/TEST-PLAN.md`，用例编号回填到各步骤。写作中定下的跨检查点修正：CP0 布局加顶部工具栏；术语表光猫端口改为 `wan` + `lan1`、AP 客户端口 `wlan1…N`；CP1 的 ProbeResult 加 `traceroute` / `hops` / `fixAt.portId`；新增验收方式【网页测试】
- 2026-09-04 CP0 完成并打勾：27 条用例全过，首次提交 77c177f，私有仓库 https://github.com/CherryLover/virtual-net 。偏差记在 CP0 文档「实施记录」
- 2026-09-04 CP1 完成并打勾：62 条用例全过（引擎 51 条自动测试 + 页面场景），偏差记在 CP1 文档「实施记录」
- 2026-09-04 CP1 试用调整：选中设备可直接一键 ping 网关 / 外网 / 打开网站；设备加线条图标。用例 T-CP1-063–068 全过
- 2026-09-05 CP2 完成并打勾：66 条用例全过，98 条自动测试。偏差记在 CP2 文档「实施记录」
- 2026-09-05 CP3 完成并打勾：58 条用例全过，自动测试 141 条。偏差记在 CP3 文档「实施记录」
- 2026-09-05 CP4 细化文档完成（53 条用例）。分区改为「由拓扑推导、只做视觉」，防火墙落成透明设备节点；总纲 CP4 第一条措辞相应调整
- 2026-09-05 公开体验版上线 https://vnet.flyooo.uk （Cloudflare Workers 静态资源 + 自定义域名，`pnpm deploy` 发布）。首次打开载入示例拓扑（路由器 + 两台电脑 + 互联网），新手引导弹窗（工具栏「帮助」可重开），工具栏加「示例」按钮，补 favicon。用例 T-PUB-001–008

---

## 后续需求补充：企业网络与服务演练（2026-09-07）

> 本节整理已讨论的方向，作为后续细化依据，不代表功能已实现或已排期。保留既有检查点编号和完成记录；开始任何新批次前，仍先确认范围、编写细化方案与验收场景，再实施。本文不是交接文档，也不替代 Task Atlas 长期待办系统。

### 1. 当前进度与执行顺序

- CP0–CP4 已完成；CP5 的服务器、HTTP / CONNECT / SOCKS5 TCP 代理首批已完成，整个 CP5 尚未完成。CP6、CP7 未开始。
- 页面与连线优化、名称位置调整、紧凑步骤标记及节点库分组已完成并上线。节点库分为设备与主机、软件与服务、网络环境，具体规则见 [设计文档](docs/design/network-model-and-ui.md)。
- 下一步仍先细化 CP5 后续的具体批次，再推进 CP6、CP7；本节新增协议不自动并入 CP5，避免不断扩大该阶段范围。
- 节点复制与成组建议作为独立画布优化批次，在 CP6 前安排；这是候选插入位置，正式调整执行顺序时再确认。
- 前文旧验收数字与发布记录属于当时的证据；节点库调整后的 258 项测试及页面验证见 [测试表](docs/TEST-PLAN.md)。新增需求不据此标为完成。

### 2. 产品定位与模拟边界

逐步将主要受众聚焦于网络工程师、企业内部运维人员，同时保留现有家庭与小型网络使用场景。核心价值从“网络能不能通”扩展到“网络与服务为什么出问题、修改会影响什么”。

- 参考 Cisco Packet Tracer 的部分协议与演练能力，不承诺完整复刻、思科命令兼容或运行真实设备系统。
- 区分三种结果：网络路径可达、服务连接成功、业务操作成功。不能只因端口可达就判断邮件已投递或应用工作正常。
- 当前“访问页面”主要验证解析与连接，不提供真实网页内容或完整 TLS 握手。后续能力必须声明模拟深度，不以协议名称代替实际行为。
- 保留通用、中性的节点与规则表达，不按地域或特定产品决定网络行为。
- 前文暂不做命令行、动态路由及延迟丢包的边界目前继续有效；相关候选获批时再修改范围，不因参考 Packet Tracer 自动解除限制。

### 3. 服务与协议扩展

优先做可解释、可重复的完整场景，而不是追求协议数量。设备承载服务，协议属于服务配置；不为每个协议都新增一种节点。邮件服务器等可考虑作为服务器预设入口，同一服务器允许提供多项服务。

| 建议批次 | 候选能力 | 可验证的问题 |
| --- | --- | --- |
| 通用服务检查 | 指定端口连接、DNS 记录查询、基本 TLS 连接检查 | 路径被阻断、服务未开启、解析错误、证书或加密方式不匹配 |
| 邮件首批 | SMTP 发信、IMAP 收信，同一邮件服务器内的投递与读取 | 认证失败、收件人不存在、服务关闭、能发不能收 |
| 邮件后续 | 跨服务器投递、DNS MX 记录、转发限制；POP3 按需求补充 | 找不到收件域服务器、投递路径失败、禁止转发 |
| 企业常用服务 | NTP 时间同步、Syslog 日志上报、SNMP 设备监控、SSH 远程管理等 | 按实际运维场景选择与细化，不承诺一次全部支持 |
| 网络协议深化 | IPv6、生成树、动态路由及故障切换 | 环路、路径选择、链路变化后的恢复；另设阶段，不作为普通服务选项处理 |

邮件首批建议场景：员工电脑向同一服务器上的另一模拟邮箱发信，收件方通过 IMAP 读取。结果分别展示连接、认证、提交、投递和读取，任何阶段失败都保留原因；“服务器接受邮件”与“收件人已收到”不能混为一谈。跨服务器投递及加密流程后续分别细化。

所有邮件、账号和业务数据仅用于模拟，不向真实邮箱发送邮件、不连接真实业务系统，不填写真实密码。TLS 可先模拟握手条件与失败原因，不代表实施真实加密通信。

### 4. 协议扩展所需的共同能力

- 交互过程：逐步支持建立连接、认证、响应、等待与失败；超时、重试和状态随时间变化的规则必须明确，不能用固定成功文案代替。
- 运行状态：按实际支持程度查看路由、地址分配、连接记录和服务日志，配置与运行结果分开呈现。
- 故障演练：停用链路或服务、修改规则，比较调整前后的路径与业务结果；后续支持一组关键服务的批量验证。
- 结果解释：既说明发生了什么，也定位到节点、配置或协议阶段；缺失数据和未支持的行为标为未知，不当作通过。
- 第一批先列出模拟深度、状态重置方式、可重复条件及失败场景，再决定是否需要更完整的时间推进机制；已有路径动画不等同于真实时序模拟。

### 5. 节点复制与持久分组

已确认需求方向为单节点复制、多选节点成组及联动拖动；多节点复制作为配套建议一并保留。下列交互约定是后续细化的默认建议，尚未实施。

| 功能 | 建议行为 |
| --- | --- |
| 单节点复制 | 保留配置，生成独立节点，错开放置；默认不复制对外连线 |
| 多节点复制 | 复制所选节点及它们之间的连线，不复制通向未选中节点的连线 |
| 成组与移动 | 多选后建立可命名分组，拖动组边框时成员联动；取消选择、保存、刷新后仍保留组关系 |
| 组内编辑 | 点击边框选组，点击成员单独配置；成员可在组内调整位置，可加入或移出成员 |
| 解组 | 仅移除组关系，不删除节点和连线 |
| 撤销 | 复制、成组、解组和整组移动分别作为一次可撤销操作 |

- 现有多选拖动只是临时选择，不等于这里的持久分组。
- 分组用简洁边框与组名表示，只用于画布整理，不创建 VLAN、子网、访问隔离或虚拟网络。
- 复制时重新生成节点身份和需要唯一的设备标识，避免克隆 MAC 等身份引入意外问题；手动 IP 原样保留并提示冲突，不擅自改网段、网关或路由。自动分配的地址按原有分配规则重新获得。
- 第一版不做嵌套分组。保存与导入导出应保留组关系，新程序读取旧文件时保持原有无分组行为。
- 细化时补齐边界：组删除与成员删除的区别、部分成员被选中时的复制规则、跨组连线和手动曲线的移动行为、组边界调整方式。

### 6. MCP 与真实网络对照：暂不纳入

- 用户已确认当前不加入 MCP，不开发本地连接程序、线上 MCP 服务或网页实时同步，不为此引入账号、后端或额外安装步骤。
- 纯静态网页或单个网页文件本身不能独立充当供外部 AI 工具连接的常驻 MCP 服务；借助本地程序或线上服务可以实现，但不符合当前打开即用的使用取舍。这是本轮范围决定，不代表技术上永久不可行。
- 保留讨论动机：未来希望 AI 能管理网络图、执行验证，并与真实网络结构作对照；目前不是待实施承诺。
- 若将来重新讨论，先区分配置结构对照与实际访问结果对照。真实采集应来自用户授权的位置和范围，优先只读，不自动修改真实设备。
- 模拟通过不代表真实网络必然正常；采集不完整、设备状态、线路质量和未支持协议均需明确说明。当前不开展真实网络采集或自动校验。

### 7. 后续细化前需要明确的内容

- 第一批企业使用场景：服务故障排查、配置变更验证或网络教学，哪个优先，以场景决定协议顺序。
- 各协议做到哪一层：仅连接、完整业务步骤，还是包含超时重试与故障恢复；分别定义完成标准。
- 邮件账户、邮箱内容、连接记录等运行状态如何保存、重置和重放，避免影响已有网络图兼容。
- 节点复制与成组批次的具体插入时间，以及前述删除、部分选择、连线和曲线边界。
- 后续依照“范围确认 → 细化设计 → 验收用例 → 实现与验证”的顺序推进；本次仅追加需求，不创建新的已排期阶段。

### 8. 画布导出为图片或 PDF

用户确认后续增加画布文件导出，支持 SVG、PNG、PDF 三种格式；本次仅记录需求，不立即实现。

- 导出完整画布内容，包括节点、连线、名称、标签，以及后续实现的分组边框和组名；不能只截取屏幕上当前可见的一部分。
- 不包含顶部工具栏、左右操作面板、选中框、拖拽控制柄等编辑控件。
- SVG 放大后保持清晰；PNG 输出清晰图片；PDF 便于查看、打印和分享。
- 保留原有 JSON 导入导出：JSON 用于继续编辑网络图，SVG、PNG、PDF 用于展示，不承诺将这三种文件重新导入为可编辑网络图。
- 后续细化时再确定 PNG 清晰度、背景选项和 PDF 纸张适配；验收需检查中文、图标、曲线和画布边缘内容完整、不被裁切，导出不改变原图。
- 本轮随后提出但未获采纳的额外功能建议不加入需求范围，不影响前面已确认的内容。
