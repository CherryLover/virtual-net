# virtual-net 路线图

> 状态：进行中 · 建档 2026-09-04
> 这是项目总纲。阶段目标、范围、完成标准都以这里为准。改动请在文末「变更记录」留一笔。

## 一句话

在浏览器里搭一个"虚拟配网"沙盘：拖设备、连网线、配 IP，然后直接验证"能不能通、在哪断、为什么"。
以通用方式表达家庭、工作室和小公司的网络：终端、网络设备、服务器、访问控制、代理与虚拟网络。地域和具体产品只作为可选场景，不决定基础模型。

> 2026-09-07 方向修订：详见 [通用网络模型与页面优化](docs/design/network-model-and-ui.md)。本次只更新文档，未实现新功能。CP4 旧细化文档与旧验收用例保留历史参考，冲突内容不再作为开发依据。

## 面向谁

- 家里喜欢折腾网络的人
- 工作室 / 小公司的网络运维
- 做跨境业务的公司

## 第一期不做什么

- 不做登录和账号。打开页面就能用，数据存在浏览器本地
- 不导出真实设备的配置文件。只在页面上管理和查看
- 不模拟命令行。全部表单配置
- 不模拟带宽、延迟、丢包。"能通"只代表配置逻辑上通得过
- 不做动态路由协议（OSPF / BGP / 生成树）

## 怎么读这份文档

- 「总览」里一个大 checkbox 就是一个**检查点**，代表一个能单独演示的阶段目标
- 每个检查点的详情包含：**目标**、**拆分步骤**、**完成标准**
- 完成标准全部满足、引擎自动测试通过、页面实际点过一遍，才能打勾
- 每个检查点动手前，先在 `docs/checkpoints/` 下写细化文档把小功能拆细，讨论完再开工。文档规范见 [docs/checkpoints/README.md](docs/checkpoints/README.md)
- 细化文档里每个步骤都带验收标准。所有验收标准汇总成一份独立的测试表 [docs/TEST-PLAN.md](docs/TEST-PLAN.md)，从上到下逐条核对
- 顺序可以调、范围可以改，但要记进「变更记录」

---

## 总览

- [x] **CP0 项目骨架** — 仓库、引擎与网页两个包、测试跑通、空画布能打开 · [细化文档](docs/checkpoints/CP0-project-skeleton.md)
- [x] **CP1 局域网基础** — 电脑 ↔ 路由器 ↔ 外网能通，配错能报出来 · [细化文档](docs/checkpoints/CP1-lan-basics.md)
- [x] **CP2 交换机与多设备** — 交换机、VLAN、光猫、无线 AP、多台电脑 · [细化文档](docs/checkpoints/CP2-switching-and-devices.md)
- [x] **CP3 路径追踪与动画** — 逐跳显示数据包怎么走、在哪停、为什么 · [细化文档](docs/checkpoints/CP3-trace-and-animation.md)
- [ ] **CP4 网络区域与访问控制** — 通用区域、访问规则、目标服务、DNS 应答处理；旧细化方案待重写
- [ ] **CP5 服务器、代理与转发** — 左侧独立代理实体、HTTP / SOCKS 等代理、客户端选择与多段路径；协议分批范围待定
- [ ] **CP6 虚拟网络与网关组合** — 虚拟化主机、网桥、虚拟设备与网关职责
- [ ] **CP7 从外面进来** — 端口转发、公网 IP 判断、从外部访问家里的 NAS
- [ ] **UX 页面与连线优化** — 保留主画布，调整外围布局、右侧面板、可拖拽贝塞尔曲线与端口位置 · [设计需求](docs/design/network-model-and-ui.md)

---

## CP0 项目骨架

> 细化文档：[docs/checkpoints/CP0-project-skeleton.md](docs/checkpoints/CP0-project-skeleton.md)

**目标**：把地基打好。一条命令能启动网页看到空画布，一条命令能跑引擎测试。

**拆分步骤**
- [ ] pnpm workspace，两个包：`packages/engine`（纯逻辑）和 `apps/web`（网页）
- [ ] 引擎：TypeScript + vitest，放一个占位测试
- [ ] 网页：React + Vite + React Flow，打开是一块空画布和一个空的左侧设备栏
- [ ] 统一的代码格式化与检查
- [ ] README 写清楚怎么启动、怎么测试

**完成标准**
1. `pnpm dev` 打开浏览器看到空画布
2. `pnpm test` 通过
3. 目录结构和本文档「目录结构」一节一致

---

## CP1 局域网基础

> 细化文档：[docs/checkpoints/CP1-lan-basics.md](docs/checkpoints/CP1-lan-basics.md)

**目标**：最小可用闭环。一台电脑、一台路由器、一个外网。电脑能 ping 通路由器，能 ping 通外网，能打开网站。配错了能指出哪里错。

**拆分步骤**

数据模型
- [ ] 设备、端口、连线、IP 配置的统一数据结构，整张图能序列化成一份 JSON
- [ ] 第一批设备：电脑、路由器、互联网

画布
- [ ] 从左侧设备栏拖设备进画布
- [ ] 在两个端口之间拉线，线上显示端口名
- [ ] 选中、移动、删除设备和连线
- [ ] 点设备打开右侧配置面板

配置面板
- [ ] 电脑：IP、掩码、网关、DNS。手动填 / 自动获取
- [ ] 路由器：LAN 口 IP 和掩码、DHCP 开关与地址池、WAN 口（自动获取公网地址）、NAT 默认开
- [ ] 互联网：内置几个目标（8.8.8.8、一个国内网站、一个国外网站）。本阶段一律视为可达

引擎
- [ ] ARP 与直连二层转发
- [ ] IP 转发与默认路由
- [ ] NAT（源地址转换）
- [ ] DHCP 分配
- [ ] 每一步都产出"决策记录"，供结果面板和后续动画使用

验证
- [ ] ping：任选两点，返回 通 / 不通、断在哪、原因
- [ ] 访问网站：电脑到某个目标网站，先 DNS 再连接
- [ ] 静态检查：IP 冲突、网关不在本网段、没配网关、一根线两端不在同一网段、掩码不一致、DHCP 地址池与静态地址重叠
- [ ] 结果面板：列出所有问题，点一条定位到对应设备

保存
- [ ] 浏览器本地自动保存，刷新不丢
- [ ] 导出 JSON、导入 JSON
- [ ] 新建 / 清空

**完成标准（验收场景）**
1. 空画布拖入 1 电脑 + 1 路由器 + 1 互联网，连两根线。电脑设自动获取，路由器开 DHCP。ping 路由器通，ping 8.8.8.8 通，打开国外网站成功
2. 把电脑网关改成别的网段。静态检查报"网关不在本网段"，ping 8.8.8.8 不通，原因指向网关
3. 关掉路由器 NAT。ping 外网不通，原因说明私网地址无法回程
4. 导出 JSON，清空画布，再导入，图和配置完整回来
5. 以上场景在引擎里各有一条自动测试

---

## CP2 交换机与多设备

> 细化文档：[docs/checkpoints/CP2-switching-and-devices.md](docs/checkpoints/CP2-switching-and-devices.md)

**目标**：能搭出一个真实的家庭或小办公室网络。

**拆分步骤**
- [ ] 二层交换机：多口、MAC 地址学习、未知目标泛洪
- [ ] VLAN：access 口 / trunk 口。路由器多 LAN 口或单臂路由
- [ ] 无线 AP：本阶段当作带 SSID 标签的交换机，无线客户端按有线处理
- [ ] 光猫：桥接 / 路由两种模式。拨号在光猫还是在路由器。给的是公网 IP 还是运营商内网地址
- [ ] DHCP 多个地址池，每个 VLAN 一池
- [ ] 静态检查：VLAN 两端不一致、trunk 漏放行、双层 NAT 提示、同一网段两个 DHCP 服务
- [ ] 画布：设备多了以后的对齐、框选、批量移动

**完成标准（验收场景）**
1. 光猫桥接 + 路由器拨号 + 交换机 + 3 台电脑 + 1 个 AP，全部互通且能上外网
2. 光猫改成路由模式，静态检查提示双层 NAT
3. 两个 VLAN 各挂一台电脑，同 VLAN 通，跨 VLAN 不通。加上单臂路由后跨 VLAN 通

---

## CP3 路径追踪与动画

> 细化文档：[docs/checkpoints/CP3-trace-and-animation.md](docs/checkpoints/CP3-trace-and-animation.md)

**目标**：把"为什么通 / 为什么不通"变成看得见的东西。这是 Packet Tracer 最有价值的部分。

**拆分步骤**
- [ ] traceroute：逐跳列表
- [ ] 动画：数据包沿连线移动，逐跳停留，失败点高亮
- [ ] 点某一跳看包头：源 / 目的 MAC、源 / 目的 IP、NAT 前后、VLAN 标签
- [ ] 每一跳的决策解释：查路由表命中哪条、ARP 查到谁、NAT 做了什么映射
- [ ] 播放 / 单步 / 调速

**完成标准（验收场景）**
1. CP1 场景 1 的 ping 8.8.8.8 能逐跳播放，路由器那一跳显示 NAT 前后地址
2. CP1 场景 2 的失败 ping 停在电脑上，解释是"网关不可达"

---

## CP4 网络区域与访问控制

> 修订依据：[设计文档第 2–3 节](docs/design/network-model-and-ui.md)。[旧细化文档](docs/checkpoints/CP4-zones-and-border.md)仅供历史参考，须重写后再开工。

**目标**：以通用规则解释某次访问为什么允许或拒绝，不依赖地域或固定网站名单。

**拆分步骤**
- [ ] 网络区域与目标服务：可命名区域、可配置目标；位置只影响展示
- [ ] 访问控制：实际经过的设备逐台检查，规则支持允许、拒绝和明确的默认动作
- [ ] 明确规则顺序、返回连接处理与设备可观察的信息范围
- [ ] DNS 服务与查询路径分开建模，区分服务自身改写和途中应答处理
- [ ] 诊断显示命中设备、规则、原目标与实际结果，不把应答改写直接等同于无法连接

**验收候选（详细规则待定稿）**
1. 同一访问只因规则开关变化而允许或拒绝，说明命中的规则
2. 两台访问控制设备均按路径生效，未经过的设备不影响结果
3. DNS 应答改写与后续连接分别展示；拖动节点不改变结果

---

## CP5 服务器、代理与转发

> 修订依据：[设计文档第 4 节](docs/design/network-model-and-ui.md)。先补服务基础，再细化协议与分批范围。

**目标**：用户可从左侧添加“代理”实体，选择通过它访问目标，并看到客户端到代理、代理到目标两段连接的结果。

**拆分步骤**
- [ ] 服务器与服务基础：地址、监听端口、启停、身份验证和访问限制
- [ ] 左侧独立代理入口；节点可连线、配置、保存和诊断，内部复用主机能力
- [ ] HTTP、CONNECT、SOCKS5 等协议；建议首批 TCP，UDP 与隧道范围另行确认
- [ ] 客户端如何选择代理、本地或代理端解析、失败后的回退策略
- [ ] 分段追踪与原因：入口不可达、认证失败、解析失败、出口不可达分别定位
- [ ] 分流、加密传输、隧道与多级中转分批细化，不把所有代理等同于隧道

**验收候选（协议范围待定稿）**
1. 左侧拖入代理，连接与配置后显式选择使用，访问展示两段完整路径
2. 错误认证、关闭入口端口、阻断代理出口分别给出不同失败原因
3. 本地解析和代理端解析显示不同查询位置，普通 ping 不因应用代理而自动改道
4. 图中存在但未被选用的代理不影响直接访问，也不自动消除告警

---

## CP6 虚拟网络与网关组合

**目标**：用通用虚拟化主机、网桥与网关职责，表达虚拟设备和多级转发，不绑定具体产品。

**拆分步骤**
- [ ] 虚拟化主机：物理接口、网桥、虚拟设备及其网卡，支持 VLAN
- [ ] 内部视图编辑虚拟连接，外部视图呈现物理接口
- [ ] 虚拟网关连接上游与局域网，或作为现有网关之外的额外转发节点
- [ ] 复用 CP5 服务器与服务能力表达文件服务等设备
- [ ] 检查网桥误接、网关自指、地址分配冲突和回程路径问题

**验收候选（细化文档待写）**
1. 虚拟网关正确连接后，客户端能访问目标服务
2. 额外网关承担指定代理转发，路径显示其职责
3. 网桥或网关配置错误时定位到实际错误位置

---

## CP7 从外面进来

**目标**：回答"在外面能不能访问家里的 NAS"。

**拆分步骤**
- [ ] 路由器端口转发规则
- [ ] 公网 IP 判断：光猫给的是公网还是运营商内网
- [ ] 从其他网络的设备发起访问目标服务端口
- [ ] 运营商内网时提示无法直连，建议内网穿透（第二期）

**完成标准（验收场景）**
1. 有公网 IP + 端口转发正确，外部访问 NAS 成功
2. 运营商内网地址，外部访问失败，原因指向运营商内网

---

## UX 页面与连线优化

> 独立工作包，尚未实现；详见 [设计文档第 5–8 节](docs/design/network-model-and-ui.md)。不推翻已完成阶段，不替换用户满意的主画布。

- [ ] 工具栏、左侧分组与搜索、可收起面板和统一排版
- [ ] 右侧节点面板的信息分组、字段校验、配置与验证切换
- [ ] 贝塞尔曲线控制柄拖拽、恢复自动线形、保存与撤销
- [ ] 交换机等节点上下接线位置优化，端口身份与占用语义不变
- [ ] 连线标签、路径动画、旧文件兼容和窄屏回归

验收候选见 [测试表新增节](docs/TEST-PLAN.md#方向调整与页面优化验收候选)。先确认端口布局、曲线控制和面板方案，再编码。

## 第二期候选（不排期）

- 内网穿透：frp、Cloudflare Tunnel、Tailscale / ZeroTier 组网
- IPv6
- 双 WAN、负载与故障切换
- 跨境公司多站点：两地内网通过 WireGuard 站点互联
- 线路质量：延迟、丢包、运营商出境线路差异
- 场景模板库：一键载入典型家庭 / 工作室 / 跨境小公司
- 云端保存与分享链接（接 Pocket ID）

---

## 技术选型（暂定，可讨论）

| 部分 | 选择 | 理由 |
| --- | --- | --- |
| 语言 | TypeScript | 引擎和网页共用类型，数据格式只定义一次 |
| 仓库 | pnpm workspace | `packages/engine` + `apps/web` |
| 引擎 | 纯 TypeScript，不依赖界面 | 可测试。每个验收场景就是一条测试 |
| 测试 | vitest | 快，和 Vite 同源 |
| 网页 | React + Vite | 生态成熟 |
| 画布 | React Flow（@xyflow/react） | 节点、连线、拖拽、缩放都是现成的 |
| 状态 | Zustand | 轻，够用 |
| 存储 | IndexedDB + JSON 文件 | 无后端，本地优先 |
| 部署 | Cloudflare Workers 静态资源，https://vnet.flyooo.uk | 无后端，零维护；`pnpm deploy` 一条命令发布 |

## 目录结构（预期）

```
virtual-net/
  ROADMAP.md              本文档
  README.md
  docs/
    checkpoints/          每个检查点的细化文档（规范见其 README.md）
    design/               数据模型、引擎设计
    TEST-PLAN.md          测试表，汇总所有验收标准
  packages/engine/        模拟引擎
  apps/web/               网页
```

## 工作约定

- 引擎先于界面。每个功能先在引擎里有测试，再接界面
- 验证结果必须带原因。只说"不通"不算完成
- 界面文案极简。只说做什么，不解释为什么。错误提示例外，必须说清楚
- 每个检查点打勾前，页面实际点一遍验收场景
- 改路线图要记变更记录

## 变更记录

- 2026-09-07 用户确认通用网络模型方向、左侧独立“代理”实体和页面优化需求。修订 CP4–CP6 表述与范围，新增 UX 工作包与设计文档；旧 CP4 方案及 53 条用例标为历史待替换。保留 CP0–CP3 完成状态与检查点编号；协议批次、端口与曲线交互细节尚待确认。本次仅文档更新，未实现或部署。

- 2026-09-04 建档。确定第一期范围、八个检查点、技术选型
- 2026-09-04 CP0–CP3 细化文档完成（`docs/checkpoints/`），每步带验收标准；建立测试表 `docs/TEST-PLAN.md`，用例编号回填到各步骤。写作中定下的跨检查点修正：CP0 布局加顶部工具栏；术语表光猫端口改为 `wan` + `lan1`、AP 客户端口 `wlan1…N`；CP1 的 ProbeResult 加 `traceroute` / `hops` / `fixAt.portId`；新增验收方式【网页测试】
- 2026-09-04 CP0 完成并打勾：27 条用例全过，首次提交 77c177f，私有仓库 https://github.com/CherryLover/virtual-net 。偏差记在 CP0 文档「实施记录」
- 2026-09-04 CP1 完成并打勾：62 条用例全过（引擎 51 条自动测试 + 页面场景），偏差记在 CP1 文档「实施记录」
- 2026-09-04 CP1 试用调整：选中设备可直接一键 ping 网关 / 外网 / 打开网站；设备加线条图标。用例 T-CP1-063–068 全过
- 2026-09-05 CP2 完成并打勾：66 条用例全过，98 条自动测试。偏差记在 CP2 文档「实施记录」
- 2026-09-05 CP3 完成并打勾：58 条用例全过，自动测试 141 条。偏差记在 CP3 文档「实施记录」
- 2026-09-05 CP4 细化文档完成（53 条用例）。分区改为「由拓扑推导、只做视觉」，防火墙落成透明设备节点；总纲 CP4 第一条措辞相应调整
- 2026-09-05 公开体验版上线 https://vnet.flyooo.uk （Cloudflare Workers 静态资源 + 自定义域名，`pnpm deploy` 发布）。首次打开载入示例拓扑（路由器 + 两台电脑 + 互联网），新手引导弹窗（工具栏「帮助」可重开），工具栏加「示例」按钮，补 favicon。用例 T-PUB-001–008
