# 交互批次：画布导出与分享

## 分享预览修订（2026-09-08）

- 根据实际使用反馈，分享改为固定区域完整缩略预览，不提供原始大小或滚动；普通导出保留原始大小查看。
- 1 倍 / 2 倍只影响下载文件像素，不改变预览区域、弹窗和图像显示比例；切换时保留旧图并覆盖加载状态，新图生成完成后替换。
- 加载和失败期间不可保存过期结果；旧图资源在替换或关闭时释放，取消及快速切换不串结果。
- 分享与普通不透明输出固定使用中性画布底色 `#f8f8f8`，分享补白区域同色，品牌区仍为独立浅灰背景。
- 本次验收：桌面与手机连续切换 1 倍 / 2 倍，检查弹窗和预览尺寸稳定、无滚动、不闪露画布，并下载确认像素尺寸准确翻倍。
- 修订验收通过：16 项导出测试通过；Playwright 桌面和手机各连续切换 6 次，逐帧记录共 244 帧（含 66 帧加载状态），弹窗和预览位置/尺寸不变、始终保留图片、无横向或纵向滚动。下载尺寸为 720×771 与 1440×1542。
- 修订截图与记录：`/tmp/vnet-share-stability/desktop-1x.png`、`desktop-2x.png`、`mobile-1x.png`、`mobile-2x.png`、`report.json`；验证脚本 `/tmp/vnet-share-stability.mjs`。
- 强调色联验：海蓝强调色与旧深色填充同时存在时，实际分享 PNG 保留海蓝图标、白色节点和浅灰画布，旧深灰像素为 0。普通、超宽、超高输出分别为 1440×1542、6552×1542、1440×6182，桌面和手机六张预览均完整适配、无滚动；证据 `/tmp/vnet-share-accent/`。最终完整测试 415 项、代码检查和包含类型检查的构建通过。

## 范围与方案

- 对应 ROADMAP 第 8、10 节；SVG、PNG、PDF 普通输出，以及独立的品牌 PNG 分享预览。
- 以已渲染的完整 React Flow 画布建立只读副本，重新适配全部节点、端口标签、曲线、连线标签和分组边界。不改变工作台视口、选择、历史或保存内容。
- 副本删除编辑、选择和播放控件；不序列化拓扑对象、不嵌入账号密码或隐藏配置。仅包含画布展示文本和图形。
- SVG 使用内嵌样式的可缩放 HTML/SVG；PNG 提供 1 倍和 2 倍，PDF 使用整图自适应单页。透明背景用于普通 SVG/PNG，PDF 保留图背景、不透明，分享保留图配色。SVG 已验证浏览器独立打开，使用 foreignObject 的格式对传统矢量编辑软件的兼容性取决于软件支持。
- 分享品牌区独立浅灰背景，最小宽 720px，二维码白底四模块静区、160px，网址为 https://vnet.flyooo.uk/。长宽图不缩小品牌文字。
- 单边最大 16384 像素，总像素最大 3200 万；超限明确提示降低清晰度或缩小图，不静默裁切。空图不可生成。

## 完成标准

- [x] 纯图三个格式真实下载，SVG/PNG/PDF 打开正常。
- [x] 分享先预览再保存，二维码解码得到产品网址。
- [x] 画布外节点、中文长名称、图标、曲线、分组和配色完整。
- [x] 不包含编辑框、控制柄、面板和隐藏配置；导出不改变原图。
- [x] 空图、超限、生成失败、重试与快速切换均有正确结果。
- [x] 桌面和窄屏键盘可操作，自动测试、类型检查、整项目构建与 lint 通过；本批次主验收共 411 项测试通过。

## 首版验证记录（2026-09-08，旧配色行为已被替代）

- 14 项导出单元与交互测试通过：边界/尺寸保护、生成失败与重试、选项切换丢弃旧结果、预览完成前不可保存、关闭回收资源、原始大小查看。
- Playwright 使用独立 Chrome 上下文，1440×1000 和 390×844 两种视口；真实点击并下载 PNG、SVG、PDF，分享保存 PNG，检查原始大小/适应窗口与 Escape 关闭。
- 实际输出包括负坐标、视口外设备、300 字以上分组名、中文设备名、三个自定义颜色曲线路径；逐像素确认连线色 2385 像素、节点色 31458 像素，原图、选择、撤销历史完全未改变。
- 分组名称在导出副本中置于边框上方，自动换行并重新计算完整边界；最后一个字可见且不覆盖组内设备。暗色页面下图配色仍独立，透明 PNG 角落 alpha 为 0。
- 普通、高清、5236×385 超宽、720×4370 超高四份分享图片均用 macOS Vision 实际解码，结果为 `https://vnet.flyooo.uk/`；空图和超过 16384 像素的图明确阻止，不输出裁切文件。
- SVG 单独在浏览器打开，PDF 通过 `pdftoppm` 实际转图检查；中文、图标、分组及曲线均可见。
- 本机产物：`/tmp/vnet-ux-export/`，包括 `network.svg/png/pdf`、`share.png`、`share-ui.png`、`wide.png`、`tall.png`、`ui-network.svg/png/pdf` 及桌面/移动截图；自动化脚本 `/tmp/vnet-export-check.mjs`、`/tmp/vnet-export-downloads.mjs`，二维码检查 `/tmp/vnet-export-qr.swift`。
- `pnpm install --frozen-lockfile` 成功；明确禁止 core-js 的非必要安装脚本，未留下待确认的依赖配置。
